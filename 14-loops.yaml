- name: loops in ansible
  hosts: localhost
  tasks:
  - name: print the name
    ansible.builtin.debug:
     msg: "hellow {{item}}"
     loop:
     - sankar
     - akhila
     - chalapathi
     - bhavani