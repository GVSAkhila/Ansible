---
- name: Installing packages
  hosts: mongodb
  become: yes
  tasks:
    - name: Install the packages
      ansible.builtin.yum:
        name: "{{ item }}"
        state: installed
      loop:
        - nginx
        - postfix
        - cyrus-sasl-plain
        - mail
